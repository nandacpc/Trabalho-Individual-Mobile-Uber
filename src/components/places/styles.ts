import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    gap: 3,
    paddingVertical: 15,
  },
  placeTitle: {
    fontWeight: "bold",
    fontSize: 18,
  },
  placeAddress: {
    color: "#666",
    fontSize: 16,
  },
});
